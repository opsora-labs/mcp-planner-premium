node_modules/
dist/
*.log
.env
.env.*
.DS_Store


test/e2e/fixtures/it-planner-board.json

# Acceptance / e2e reports hold real customer task names — keep local only.
# Reports are written into reports/; the root globs catch any stray legacy output.
reports/
!reports/.gitkeep
pm-acceptance-report-*.md
e2e-report-*.md

# Seed-board cache: holds real task GUIDs/names from the fixture build — keep local
.e2e-seed-cache.json

# Claude Code: personal, per-user overrides (shared config lives in .claude/settings.json)
.claude/settings.local.json

# Cached Entra refresh token — never commit (see scripts/auth-login.ts)
.tokens.json
